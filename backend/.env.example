# =============================================================
# .env.example  —  TEMPLATE FOR ENVIRONMENT VARIABLES
# =============================================================
# HOW TO USE:
#   Copy this file:   cp .env.example .env
#   Then fill in your actual values in .env
#
# IMPORTANT: .env is in .gitignore — it will NEVER be pushed to GitHub
# This .env.example file IS pushed — it shows others what keys are needed
# =============================================================

DATABASE_URL=sqlite:///pmis.db
JWT_SECRET_KEY=your-secret-key-here
SECRET_KEY=your-flask-secret-here
FLASK_ENV=development
FLASK_DEBUG=1
